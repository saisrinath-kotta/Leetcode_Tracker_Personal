{
  "name": "dsa-master",
  "version": "1.0.0",
  "private": true,
  "description": "DSA Master - Full-stack Data Structures & Algorithms Learning Platform",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n \"server,client\" -c \"blue,green\" \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "start": "npm run start --workspace=server",
    "seed": "npm run seed --workspace=server",
    "seed:problems": "npm run seed:problems --workspace=server",
    "seed:dev": "npm run seed:dev --workspace=server",
    "validate:problems": "npm run validate:problems --workspace=server",
    "test": "npm run test --workspace=server && npm run test --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
